# 开发流程（Day 2 练习分支合并而来）

1. 激活虚拟环境：`source .venv/bin/activate`
2. 安装依赖：`pip install -r requirements.txt`
3. 运行测试：`pytest`
4. 启动服务（后续）：`python -m src.main`

分支约定：功能在 `feature/*` 开发，文档在 `docs/*`，合并回 `main`。
